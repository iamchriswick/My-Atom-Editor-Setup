import { Slice } from '../opcodes';
export declare class CompiledBlock {
    start: number;
    end: number;
    slice: Slice;
    constructor(slice: Slice);
}
export declare class CompiledProgram extends CompiledBlock {
    symbols: number;
    constructor(slice: Slice, symbols: number);
}
